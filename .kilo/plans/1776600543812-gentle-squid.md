# Plan: Clean up src-tauri Backend Codebase

## Current State
- **main.rs**: Entry point, imports commands from plugin_manager
- **lib.rs**: Contains unused `greet` function and duplicate `run` function (will remove)
- **plugin_manager.rs**: Contains all actual commands (get_packages, list_installed_packages, install_package)
- **engine-core/mod.rs**: Empty file, unused

## Issues Identified
1. Duplicate Tauri entry points (both main.rs and lib.rs have builder setup)
2. Unused code: `greet` function and `run` function in lib.rs
3. Empty module `engine-core/mod.rs` should be removed
4. No error context - all errors converted to strings lose detail
5. No docstrings/comments
6. Inconsistent async/sync: `get_packages` is async, `install_package` is blocking

## Implementation Steps (main.rs only approach)

### Step 1: Clean up lib.rs
- Remove the `greet` function
- Remove the `run` function  
- Keep minimal lib.rs or remove it if not needed for library builds

### Step 2: Clean up main.rs
- Add module documentation
- Keep as only entry point

### Step 3: Organize plugin_manager.rs
- Add module-level docstring
- Add docstrings to public functions
- Make error handling more consistent with context

### Step 4: Remove empty engine-core module
- Delete `src/engine-core/mod.rs`
- Delete the `src/engine-core/` directory if empty

## Files to Modify
- `src-tauri/src/main.rs` - Add docs
- `src-tauri/src/lib.rs` - Remove unused code, keep minimal
- `src-tauri/src/plugin_manager.rs` - Add docs, improve error handling

## Files to Delete
- `src-tauri/src/engine-core/mod.rs`
- `src-tauri/src/engine-core/` (directory if empty)